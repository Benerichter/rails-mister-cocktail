<h1><%= @cocktail.name %></h1>
<% @cocktail.doses.each do |e| %>
  <%= "#{e.description} | #{e.ingredient.name}" %>
  <%= link_to "Delete", dose_path(e), method: :delete  %>
  <br>
<% end %>
<%= link_to "Go back", cocktails_path %>

<%= link_to "Add Ingredient", new_cocktail_dose_path(@cocktail)%>
